<!--
  Exposes only the cached notice renders, and nothing else the app stores.

  `path` is relative to filesDir and matches NoticeImageStore.IMAGE_DIR, so widening it would hand
  other apps read access to the Room database sitting alongside it.
-->
<paths>
    <files-path
        name="notice_images"
        path="notice_images/" />
</paths>
